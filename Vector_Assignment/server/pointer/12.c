#include <stdio.h>
        void main()
        {
            char *s= "hello"; //charector pointer point h
            char *p = s + 2;  // p=l
            printf("%c\t%c", *p, s[1]);  // l  s[1] =e
        }
